{% extends 'base.html.twig' %}

{% block title %}Pokoj{% endblock %}

{% block body %}
    <h1>Pokoj</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pokoj.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ pokoj.nazwa }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pokoj_index') }}">bPowrót do listy</a>

    <a href="{{ path('pokoj_edit', {'id': pokoj.id}) }}">Edytuj</a>

    {{ include('pokoj/_delete_form.html.twig') }}
{% endblock %}
