{% extends 'base.html.twig' %}

{% block title %}Mebel index{% endblock %}

{% block body %}
    <h1>Mebel index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Ilosc_polek</th>
                <th>Nazwa</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for mebel in mebels %}
            <tr>
                <td>{{ mebel.id }}</td>
                <td>{{ mebel.iloscPolek }}</td>
                <td>{{ mebel.nazwa }}</td>
                <td>{{ mebel.pokoj }}</td>
                <td>
                    <a href="{{ path('mebel_show', {'id': mebel.id}) }}">show</a>
                    <a href="{{ path('mebel_edit', {'id': mebel.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('mebel_new') }}">Create new</a>
{% endblock %}
