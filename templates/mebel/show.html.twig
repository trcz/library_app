{% extends 'base.html.twig' %}

{% block title %}Mebel{% endblock %}

{% block body %}
    <h1>Mebel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mebel.id }}</td>
            </tr>
            <tr>
                <th>Ilosc_polek</th>
                <td>{{ mebel.iloscPolek }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ mebel.nazwa }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mebel_index') }}">back to list</a>

    <a href="{{ path('mebel_edit', {'id': mebel.id}) }}">edit</a>

    {{ include('mebel/_delete_form.html.twig') }}
{% endblock %}
