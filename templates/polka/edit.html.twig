{% extends 'base.html.twig' %}

{% block title %}Edytuj Półkę{% endblock %}

{% block body %}
    <h1>Edytuj Półkę</h1>

    {{ include('polka/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('polka_index') }}">Powrót do listy</a>

    {{ include('polka/_delete_form.html.twig') }}
{% endblock %}
