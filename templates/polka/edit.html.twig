{% extends 'base.html.twig' %}

{% block title %}Edit Polka{% endblock %}

{% block body %}
    <h1>Edit Polka</h1>

    {{ include('polka/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('polka_index') }}">back to list</a>

    {{ include('polka/_delete_form.html.twig') }}
{% endblock %}
