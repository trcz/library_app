<form method="post" action="{{ path('wypozyczenie_delete', {'id': wypozyczenie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wypozyczenie.id) }}">
    <button class="btn">Usuń</button>
</form>
