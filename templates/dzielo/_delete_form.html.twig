<form method="post" action="{{ path('dzielo_delete', {'id': dzielo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dzielo.id) }}">
    <button class="btn">Usuń</button>
</form>
